import { BackgroundCSSDirective } from './background-css.directive';

describe('BackgroundCSSDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundCSSDirective();
    expect(directive).toBeTruthy();
  });
});
