import { AutoplayDirective } from './autoplay.directive';

describe('AutoplayDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoplayDirective();
    expect(directive).toBeTruthy();
  });
});
